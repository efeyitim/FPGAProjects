#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  if(argc != 3)
    {
      printf("Enter only two numbers!"\n);
      return -1;
    }
  else
    {
      int x = atoi(argv[1]);
      int y = atoi(argv[1]);
    }

  printf("%d, %d", x, y);
  
}
